// página de login
import estilos from "./login.module.css";

export default function login(){
    return(
        <main className={estilos.containerMain}>

            <div className={estilos.containerLogin}>

                <form id={estilos.idFormLogin} className={estilos.formLogin}>

                </form>

            </div>

        </main>
    );
};